{
  "name": "healthstack",
  "version": "0.1.0",
  "description": "REST API for HealthStack, the medical result application for medical  laboratories.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Vladimir Fomene",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4"
  }
}
